# Copy into common/decisions and replace every MOD token and country tag.
MOD_state_program_category = {
	MOD_state_program = {
		icon = GFX_decision_generic_construction

		allowed = {
			original_tag = GER
		}

		state_target = yes
		on_map_mode = map_and_decisions_view
		target_array = ROOT.core_states

		target_trigger = {
			FROM = {
				is_fully_controlled_by = ROOT
				NOT = {
					has_state_flag = MOD_state_program_complete
				}
			}
		}

		cost = 25
		fire_only_once = yes

		complete_effect = {
			FROM = {
				set_state_flag = MOD_state_program_complete
				add_building_construction = {
					type = infrastructure
					level = 1
					instant_build = yes
				}
			}
		}

		ai_will_do = {
			base = 1
		}
	}
}
