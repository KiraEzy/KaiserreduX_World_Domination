# Copy into common/scripted_localisation and replace every MOD token.
# Call from country-scoped localisation as [ROOT.GetMODReadinessText].
defined_text = {
	name = GetMODReadinessText
	# First true branch wins. Keep thresholds from most specific/highest to lowest.
	text = {
		trigger = {
			check_variable = {
				MOD_readiness > 74
			}
		}
		localization_key = MOD_READINESS_HIGH
	}
	text = {
		trigger = {
			check_variable = {
				MOD_readiness > 24
			}
		}
		localization_key = MOD_READINESS_MEDIUM
	}
	text = {
		# An unconditional fallback is safe only as the final branch.
		localization_key = MOD_READINESS_LOW
	}
}
