# Runtime-confirmed: this consumer requires DDS; a registered PNG does not load.
# Requires gfx/loadingscreens/MOD_background.dds.
MOD_background = {
	# Optional current fields:
	# dlc_allowed = "DLC name"
	# locale = "english"
	# gfx = GFX_custom_full_resolution_background
}
